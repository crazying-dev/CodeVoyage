# CodeVoyage 用户协议

最后更新：2026-09-11

请在使用 CodeVoyage（以下简称"本服务"）前完整阅读本协议。注册、登录或继续使用本服务，即表示你已阅读、理解并同意本协议全部内容。

---

## 一、服务说明

1. 本服务由两部分组成：**服务端**（账号、任务调度、仓库绑定、GitHub 事件接收）与**本机客户端**（常驻后台，负责拉取任务、调用你自行配置的 AI、在本地对代码进行修改并提交 Pull Request）。
2. AI 能力由**你自行配置**（API Key、模型、接口地址）；本服务不提供、也不代管任何 AI 账号。
3. 本服务按"现状"提供，不承诺可用性、连续性或不中断。

## 二、账号与数据

1. 账号数据（邮箱、密码摘要）存于服务端；密码仅保存单向摘要，服务端无法还原明文。
2. 你在控制台填写的 **GitHub Token 与 LLM 配置存于服务端并加密落库**，接口只返回脱敏值；本机仅保留一份同步缓存，用于服务端不可达时继续工作。
3. 服务端加密密钥保存在服务端 `instance/credential.key`（或环境变量 `CREDENTIAL_KEY`）。密钥丢失或更换将导致已存凭据无法解密，需重新填写。
4. 退出登录会清除本机凭据缓存；你应自行保管好账号密码，并对自己账号下发生的一切操作负责。

## 三、GitHub 代理与客户端互助（重要）

为了在部分网络环境下仍能访问 GitHub，本服务提供**客户端互助代理**。**该功能默认开启。**

### 3.1 它是怎么工作的

1. 当某台客户端直连 GitHub 超时（默认重试 10 次打洞）时，会向服务端申请由**其它可连 GitHub 的客户端**代为转发；
2. 服务端只负责撮合（分配代连方、提供候选地址），打洞失败时由服务端端口做**字节中继**；
3. 代连方做的是**盲转发**：它只搬运加密字节流，**无法读取**你的 GitHub Token、代码内容或提交信息。

### 3.2 你和对方分别可见什么

| 项目 | 请求方（借用代理的人） | 代连方（提供代理的人） |
| --- | --- | --- |
| 你的 GitHub Token / 代码内容 | 可见（是你自己的） | **不可见**（TLS 端到端加密） |
| 访问的目标主机 | 自己的请求 | 可见（仅限 github.com / api.github.com） |
| 对方的 IP 地址 | 可见代连方 IP | 可见请求方 IP |
| 传输的数据量 | 自己的用量 | 被转发流量的大小 |

### 3.3 你的授权与责任

1. **作为代连方**：你同意将自己的网络出口用于转发其它用户的 GitHub 请求。这意味着对方发往 GitHub 的流量会从**你的公网 IP** 发出。若对方从事违反 GitHub 服务条款或法律法规的行为，相关 IP 记录中会出现你的地址。
2. **作为请求方**：你不得利用本代理进行攻击、扫描、批量爬取、绕过访问限制、刷量或任何违反 GitHub 服务条款的行为。由此产生的一切后果由你自行承担。
3. 代理目标被严格限制为 `github.com:443`、`api.github.com:443`、`codeload.github.com:443`、`raw.githubusercontent.com:443`，不接受任意目标的转发请求。
4. 会话凭据一次性有效，代连方在转发前会向服务端校验请求方身份；会话过期即失效。

### 3.4 如何关闭

你可以在控制台「配置」页随时关闭：

- 关闭「**使用代理**」：不再借用其它客户端的网络；
- 关闭「**作为代连节点**」：不再为其它客户端转发流量。

关闭后立即生效，本机将不再参与任何互助转发。

## 四、用户义务

1. 不得利用本服务从事任何违法违规活动，不得处理你无权处理的代码仓库。
2. 不得上传、传播恶意代码，不得利用 AI 生成用于攻击、破坏、窃取的内容。
3. 不得对服务端进行压力测试、探测、反编译或试图越权访问他人数据。
4. 你对自己仓库的 `.github/workflows` 改动、以及由此产生的 Actions 执行与费用负责。

## 五、免责与责任限制

1. AI 生成的内容可能不正确、不完整或不符合你的预期；**合并 PR 前请务必自行审查**。
2. 因网络环境、第三方服务（GitHub、AI 服务商）、不可抗力导致的中断、延迟、数据丢失，本服务不承担责任。
3. 因你自行配置的 Token 权限过大、泄露或被他人使用造成的损失，由你自行承担。
4. 在法律允许的最大范围内，本服务对你承担的责任以你实际支付的服务费用为上限（本服务免费时为 0）。

## 六、隐私

1. 服务端仅保存完成调度所必需的数据：账号、仓库绑定、关键词、下拉任务与执行结果。
2. 代理转发过程中，服务端**不解密也不存储**被转发的 TLS 内容；服务端只记录会话的存在与结果（是否打洞成功、是否走中继）。
3. 本机日志（`~/.CodeVoyage/Agent/agent.log`）仅保存在你的电脑上。

## 七、协议变更与终止

1. 本协议可能随功能变化而更新，更新后会在本页面与仓库文档中同步。
2. 若你不同意更新后的条款，应停止使用并关闭客户端；继续使用即视为接受。
3. 对滥用互助代理、攻击服务端或侵害他人权益的账号，服务端有权限制或终止其使用。

## 八、联系方式

如有疑问或投诉，请通过仓库 Issue 反馈：<https://github.com/crazying-dev/CodeVoyage/issues>
